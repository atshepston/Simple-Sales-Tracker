[project]
name = "print-market-tracker"
version = "0.1.0"
description = "Mobile-friendly 3D print inventory and sales tracker"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.118.0",
  "python-multipart>=0.0.20",
  "uvicorn>=0.37.0",
]

[tool.uv]
package = false
